# dependencies & build output
node_modules/
dist/

# env & secrets
.env
.env.*
!.env.example

# editor / OS
*.local
.DS_Store
Thumbs.db

# stray screen recordings / large captures (keep them out of git)
*.mp4

# unused 1 MB SQLite mirror of the catalogs (the app reads public/data/*.json)
game.db
