# Copy to `.env` and paste your ArtifactsMMO JWT to skip the in-app token prompt
# during development. Vite exposes this as import.meta.env.TOKEN (see vite.config.ts).
# The real `.env` is gitignored.
TOKEN=your_jwt_here
